import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>L2 — single-generate-text-block (placeholder)</div>
  </React.StrictMode>
)
