import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>L4 — Multi-Step Agent and Stream POC</div>
  </React.StrictMode>
)
