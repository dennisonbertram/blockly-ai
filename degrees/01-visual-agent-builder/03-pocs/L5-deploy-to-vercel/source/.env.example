# AI Provider API Keys — copy to .env.local and fill in real values
# NEVER commit real keys. These are placeholders only.

OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Optional: Gate live model tests (used in test/e2e-handler.test.ts)
# RUN_LIVE_MODEL_TESTS=1
